service <%= @service_name %>
{
    disable         = no
    port            = <%= @service_port %>
    socket_type     = stream
    protocol        = tcp
    wait            = no
    user            = nobody
    server          = <%= @service_cmd %>
    server_args     = <%= @service_cmd_args %>
    instances       = 1
    type            = unlisted
}
